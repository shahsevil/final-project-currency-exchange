server:
  port: 8885

spring:
  main:
    banner-mode: console

  datasource:
    url: jdbc:postgresql://localhost:5432/final-project
    username: postgres
    password: secret
#    initialization-mode: always

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
